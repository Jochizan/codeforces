#include <bits/stdc++.h>

#define db double
#define ll long long

using namespace std;

int main() {
	ios::sync_with_stdio(0);
	cin.tie(0);
	
	int n;
	cin >> n;
    cout << 1 << " " << n;
	return 0;
}
